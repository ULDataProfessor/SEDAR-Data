# Historical reference only

This package preserves the original legacy `sedar.com` scraper and CAPTCHA helper.
It targets endpoints replaced by SEDAR+ in July 2023 and is not maintained for
current production use.

Use `sedar legacy-scrape --i-know-this-is-legacy` only in isolated historical
environments.
